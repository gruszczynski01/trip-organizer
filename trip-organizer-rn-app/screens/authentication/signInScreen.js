import React from "react";
import { Text, View } from "react-native";
import LoginScreen from "react-native-login-screen";

const signInScreen = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Hello, world! signInScreenkknn</Text>
    </View>
  );
};
export default signInScreen;
