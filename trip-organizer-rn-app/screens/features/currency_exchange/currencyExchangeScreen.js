import React from "react";
import { Text, View } from "react-native";

const currencyExchangeScreen = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Hello, world! currencyExchangeScreen</Text>
    </View>
  );
};
export default currencyExchangeScreen;
